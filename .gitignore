# Internal review material — the gaps register must not reach the public repository.
gaps.json

# Build artefact: regenerated by `node build_docx.js`, and it embeds the gaps register.
climate_health_catalogue.docx

node_modules/
